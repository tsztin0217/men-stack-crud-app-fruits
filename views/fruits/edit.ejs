
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/style.css">
    <script defer src="/js/script.js"></script>
    <title>Create a Fruit</title>
  </head>
<!-- views/fruits/new.ejs -->

<body>
    <!-- update the header -->
    <h1>Edit <%= fruit.name %>
    </h1>
    <!-- updated form -->

    <form action="/fruits/<%=fruit._id%>?_method=PUT" method="POST">
        <label for="name">Name:</label>
        <input type="text" name="name" value="<%= fruit.name %>" />

        <label for="isReadyToEat">Ready to Eat?</label>
        <input type="checkbox" name="isReadyToEat" <% if (fruit.isReadyToEat) { %>checked<% } %> >
        <button type="submit">Update Fruit</button>
    </form>
    <a class="fruits-index-link" href="/fruits"><div class="index-link-text">Back to Fruits</div></a>
</body>
  
</html>
